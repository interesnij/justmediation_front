/** Attorney's client from QuickBooks service. */
export interface QuickbooksClient {
  /** Id. */
  id: number;
  /** First name. */
  firstName: string;
  /** Last name. */
  lastName: string;
  /** Display name. */
  displayName: string;
  /** Email. */
  email: string;
  /** Company name. */
  companyName: string;
}
