/**
 * Author model.
 */
export interface Author {
  /** Id. */
  id: number;
  /** First Name. */
  firstName: string;
  /** Last Name. */
  lastName: string;
  /** Email. */
  email: string;
  /** Avatar. */
  avatar: string;
}
