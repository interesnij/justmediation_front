import { Message } from "./Message";

/**
 * Unsupported chat message.
 * Presents message that is not supported with current application version.
 */
export interface UnsupportedMessage extends Message {}
